[project]
name = "ios-lockscreen-chatgpt-usage"
version = "0.1.0"
description = "Read-only ChatGPT usage bridge for an iOS Scriptable lock-screen widget"
requires-python = ">=3.11"

[tool.uv]
package = false
